<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="androidlab2017.epam.com">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.fullscreen.FullscreenAlarmActivity"
                  android:label="@string/test"
                  android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ui.ringtone_list.RingtoneListActivity"
                  android:label="@string/test"
                  android:theme="@style/AppTheme"
            />

        <service android:name=".service.AlarmService"/>

        <receiver android:name=".receiver.AlarmReceiver"/>

        <receiver android:name=".receiver.ActionsNotifReceiver"/>
    </application>

</manifest>